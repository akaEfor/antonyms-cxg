`adj-wn-single-token.csv` 
`noun-wn-single-token.csv` 
`verb-wn-single-token.csv` 

These contain antonyms filtered from original Nguyen et al. source files and which are represented as single tokens in the RoBERTa-large vocabulary.

---

`all-wn-single-token.csv` 

Combines the individual parts-of-speech, after de-duping within and across files and checking for dups in the core pairs set.

---
